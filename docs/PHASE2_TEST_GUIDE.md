# Phase 2 테스트 가이드

이 문서는 Phase 2의 검색/필터링, 정렬, 필터 저장 기능에 대한 테스트 가이드를 제공합니다.

## 📋 목차

1. [검색/필터링 테스트](#검색필터링-테스트)
2. [정렬 기능 테스트](#정렬-기능-테스트)
3. [필터 저장 및 복원 테스트](#필터-저장-및-복원-테스트)

---

## 검색/필터링 테스트

### 테스트 항목: 각 필터 조합으로 검색 결과 확인

#### 1. 부서별 필터 테스트

**절차:**
1. 메인 페이지(`/`) 접속
2. 필터 패널에서 "부서" 드롭다운 선택
3. 특정 부서 선택 (예: "개발팀")
4. 검색 결과 확인

**예상 결과:**
- 선택한 부서의 직원만 표시됨
- URL에 `?department=개발팀` 파라미터 추가됨
- 직원 카드에 해당 부서 정보가 표시됨

#### 2. 직급별 필터 테스트

**절차:**
1. 필터 패널에서 "직급" 드롭다운 선택
2. 특정 직급 선택 (예: "사원")
3. 검색 결과 확인

**예상 결과:**
- 선택한 직급의 직원만 표시됨
- URL에 `?rank=사원` 파라미터 추가됨

#### 3. 재직 상태 필터 테스트

**절차:**
1. 필터 패널에서 "상태" 드롭다운 선택
2. 각 상태별로 테스트:
   - 재직 (active)
   - 휴직 (inactive)
   - 퇴사 (resigned)

**예상 결과:**
- 선택한 상태의 직원만 표시됨
- 퇴사 상태는 `/resigned` 페이지로 이동

#### 4. 입사일 범위 필터 테스트

**절차:**
1. 필터 패널에서 "입사일 시작" 날짜 선택
2. "입사일 종료" 날짜 선택
3. 검색 결과 확인

**예상 결과:**
- 선택한 날짜 범위 내 입사한 직원만 표시됨
- URL에 `?hireDateFrom=YYYY-MM-DD&hireDateTo=YYYY-MM-DD` 파라미터 추가됨

#### 5. 복합 필터 테스트

**절차:**
1. 여러 필터를 동시에 적용:
   - 부서: "개발팀"
   - 직급: "사원"
   - 상태: "재직"
   - 입사일 범위: 2020-01-01 ~ 2024-12-31
2. 검색 결과 확인

**예상 결과:**
- 모든 조건을 만족하는 직원만 표시됨
- URL에 모든 필터 파라미터가 포함됨

#### 6. 검색어와 필터 조합 테스트

**절차:**
1. 검색어 입력 (예: "홍길동")
2. 필터 적용 (예: 부서 "개발팀")
3. 검색 결과 확인

**예상 결과:**
- 검색어와 필터 조건을 모두 만족하는 직원만 표시됨
- URL에 `?search=홍길동&department=개발팀` 파라미터 추가됨

#### 7. 필터 초기화 테스트

**절차:**
1. 여러 필터 적용
2. "필터 초기화" 버튼 클릭
3. 검색 결과 확인

**예상 결과:**
- 모든 필터가 초기화됨
- 전체 직원 목록이 표시됨
- URL에서 필터 파라미터가 제거됨

---

## 정렬 기능 테스트

### 테스트 항목: 정렬 기능 동작 확인

#### 1. 이름순 정렬 테스트

**절차:**
1. 정렬 드롭다운에서 "이름순" 선택
2. 오름차순/내림차순 토글 버튼 클릭
3. 검색 결과 확인

**예상 결과:**
- **오름차순**: 가나다순 또는 알파벳순으로 정렬
- **내림차순**: 역순으로 정렬
- URL에 `?sortField=name&sortOrder=asc` 또는 `desc` 파라미터 추가됨

#### 2. 입사일순 정렬 테스트

**절차:**
1. 정렬 드롭다운에서 "입사일순" 선택
2. 오름차순/내림차순 토글
3. 검색 결과 확인

**예상 결과:**
- **오름차순**: 입사일이 빠른 순서대로 정렬
- **내림차순**: 최근 입사한 순서대로 정렬
- URL에 `?sortField=hire_date&sortOrder=asc` 또는 `desc` 파라미터 추가됨

#### 3. 급여순 정렬 테스트

**절차:**
1. 정렬 드롭다운에서 "급여순" 선택
2. 오름차순/내림차순 토글
3. 검색 결과 확인

**예상 결과:**
- **오름차순**: 급여가 낮은 순서대로 정렬
- **내림차순**: 급여가 높은 순서대로 정렬
- URL에 `?sortField=current_salary&sortOrder=asc` 또는 `desc` 파라미터 추가됨

#### 4. 부서순 정렬 테스트

**절차:**
1. 정렬 드롭다운에서 "부서순" 선택
2. 오름차순/내림차순 토글
3. 검색 결과 확인

**예상 결과:**
- 부서명 기준으로 정렬됨
- URL에 `?sortField=department&sortOrder=asc` 또는 `desc` 파라미터 추가됨

#### 5. 정렬과 필터 조합 테스트

**절차:**
1. 필터 적용 (예: 부서 "개발팀")
2. 정렬 적용 (예: 급여순 내림차순)
3. 검색 결과 확인

**예상 결과:**
- 필터링된 결과가 선택한 정렬 기준으로 정렬됨
- URL에 필터와 정렬 파라미터가 모두 포함됨

#### 6. 페이지네이션과 정렬 테스트

**절차:**
1. 정렬 적용
2. 다음 페이지로 이동
3. 정렬 상태 유지 확인

**예상 결과:**
- 다음 페이지에서도 동일한 정렬 기준이 유지됨
- URL 파라미터가 유지됨

---

## 필터 저장 및 복원 테스트

### 테스트 항목: 필터 저장 및 복원 확인

#### 1. 필터 프리셋 저장 테스트

**절차:**
1. 필터 패널에서 여러 필터 적용:
   - 부서: "개발팀"
   - 직급: "사원"
   - 입사일 범위 설정
2. 정렬 설정 (예: 급여순 내림차순)
3. 검색어 입력 (선택사항)
4. "필터 저장" 버튼 클릭
5. 프리셋 이름 입력 (예: "개발팀 사원")
6. 저장 확인

**예상 결과:**
- Toast 메시지로 저장 성공 알림 표시
- 필터 프리셋 목록에 새 프리셋 추가됨
- localStorage에 프리셋 데이터 저장됨
- 페이지 새로고침 후에도 프리셋이 유지됨

#### 2. 필터 프리셋 적용 테스트

**절차:**
1. 저장된 프리셋 목록 확인
2. 특정 프리셋 클릭
3. 필터 및 정렬이 적용되는지 확인

**예상 결과:**
- 선택한 프리셋의 필터가 자동으로 적용됨
- 정렬 기준도 자동으로 적용됨
- 검색어도 복원됨 (저장된 경우)
- URL 파라미터가 자동으로 업데이트됨
- Toast 메시지로 적용 성공 알림 표시

#### 3. 필터 프리셋 삭제 테스트

**절차:**
1. 저장된 프리셋 목록 확인
2. 삭제할 프리셋의 삭제 버튼 클릭
3. 확인 대화상자에서 확인
4. 프리셋 목록에서 제거 확인

**예상 결과:**
- 프리셋이 목록에서 제거됨
- Toast 메시지로 삭제 성공 알림 표시
- localStorage에서도 제거됨
- 페이지 새로고침 후에도 삭제 상태 유지됨

#### 4. 여러 프리셋 저장 및 관리 테스트

**절차:**
1. 여러 개의 다른 필터 조합을 프리셋으로 저장
2. 각 프리셋을 순차적으로 적용하여 정확성 확인
3. 프리셋 이름 중복 저장 시도

**예상 결과:**
- 여러 프리셋이 독립적으로 저장됨
- 각 프리셋이 정확하게 적용됨
- 중복 이름 저장 시 경고 메시지 표시

#### 5. URL 파라미터와 프리셋 연동 테스트

**절차:**
1. 필터 및 정렬 적용
2. 프리셋으로 저장
3. URL을 복사하여 새 탭에서 열기
4. 필터 상태가 URL에서 복원되는지 확인

**예상 결과:**
- URL 파라미터로 필터 상태가 복원됨
- 저장된 프리셋과 URL 파라미터가 일치함

#### 6. 브라우저 새로고침 후 프리셋 유지 테스트

**절차:**
1. 필터 프리셋 저장
2. 브라우저 새로고침 (F5)
3. 프리셋 목록 확인

**예상 결과:**
- 저장된 프리셋이 그대로 유지됨
- localStorage에서 데이터가 정상적으로 로드됨

---

## 🐛 발견된 이슈 및 해결 방법

### 이슈 1: 필터 적용 시 페이지네이션 초기화 안 됨

**증상:** 필터 적용 후에도 이전 페이지 번호가 유지됨

**해결:** 필터 변경 시 `setCurrentPage(1)` 호출 확인

### 이슈 2: URL 파라미터와 필터 상태 불일치

**증상:** URL 파라미터는 있지만 필터 UI가 반영되지 않음

**해결:** `useSearchParams`로 URL에서 초기 상태 로드 확인

---

## ✅ 테스트 체크리스트

- [ ] 부서별 필터 동작 확인
- [ ] 직급별 필터 동작 확인
- [ ] 재직 상태 필터 동작 확인
- [ ] 입사일 범위 필터 동작 확인
- [ ] 복합 필터 동작 확인
- [ ] 검색어와 필터 조합 동작 확인
- [ ] 필터 초기화 동작 확인
- [ ] 이름순 정렬 동작 확인
- [ ] 입사일순 정렬 동작 확인
- [ ] 급여순 정렬 동작 확인
- [ ] 부서순 정렬 동작 확인
- [ ] 정렬과 필터 조합 동작 확인
- [ ] 페이지네이션과 정렬 연동 확인
- [ ] 필터 프리셋 저장 동작 확인
- [ ] 필터 프리셋 적용 동작 확인
- [ ] 필터 프리셋 삭제 동작 확인
- [ ] 여러 프리셋 관리 동작 확인
- [ ] URL 파라미터와 프리셋 연동 확인
- [ ] 브라우저 새로고침 후 프리셋 유지 확인

---

## 📝 테스트 결과 기록

테스트를 완료한 후 아래 항목을 체크하세요:

**테스트 날짜:** _______________

**테스터:** _______________

**전체 테스트 결과:**
- [ ] 모든 테스트 통과
- [ ] 일부 테스트 실패 (이슈 섹션에 기록)

**비고:**
_________________________________________________
_________________________________________________

---

**마지막 업데이트:** 2024년



